// Package version is the single source of truth for the anet release version, shared by both binaries
// (anet and anet-hub) so they always report the same number.
package version

// V is the current anet release version.
const V = "0.1.5"
